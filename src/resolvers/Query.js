const users = (parent, args, context, info) => {
  // return context.prisma.links();
  return [];
};

const repos = (parent, args, context, info) => {
  // return context.prisma.links();
  return [];
};

export default {
  users,
  repos
};
