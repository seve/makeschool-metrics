type AuthPayload {
  token: String
}

type User {
  id: ID!
  username: String!
  repos: [Repo!]!
  commits: [Commit!]!
  pullRequests: [PullRequest!]!
}

type Repo {
  id: ID!
  name: String!
  description: String
  languages: [String!]!
  stars: Number
  link: String!
  commits: [Commit!]!
}

type Commit {
  id: ID!
  message: String!
  user: User!
  date: String!
  repo: Repo!
}

type Query {
  users: [User!]!
  repos: [Repo!]!
}

type Mutation {
}

type Subscription {
  newCommit: Commit
  newRepo: Repo
}